global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "we-go-backend"
    metrics_path: "/actuator/prometheus"
    scheme: http
    static_configs:
      - targets: ["host.docker.internal:9091"]